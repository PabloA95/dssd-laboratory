<%= form_with(model: protocol, local: true) do |form| %>
  <% if protocol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(protocol.errors.count, "error") %> prohibited this protocol from being saved:</h2>

      <ul>
        <% protocol.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <!-- <div class="field">
    <input type="text" name="protocol[aaa][1]" id="protocol_aaa1" value="1">
  </div>
  <div class="field">
    <input type="text" name="protocol[aaa][2]" id="protocol_aaa2" value="2">
  </div> -->
  <div class="field">
    <%= form.label :local %>
    <%= form.check_box :local %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
